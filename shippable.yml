language: node_js

node_js:
  - "0.10"

#services:
#  - selenium

build:
  ci:
    - aws iam get-user
    - pip install -U awsebcli
    - echo "Hello, World!"

integrations:
  deploy:
    - integrationName: "aws"
      type: aws
      target: eb_paas
      platform: "Node.js"
      application_name: sample-node-eb-paas
      env_name: sample-node-eb-paas-dev
      region: us-east-1
